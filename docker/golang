FROM golang:1.8

ARG userid

RUN useradd -u "$userid" autopkgupdate

RUN mkdir /go/pkg
RUN mkdir /go/src/salsa.debian.org
RUN chown -R autopkgupdate:autopkgupdate /go

USER autopkgupdate

RUN go get -u golang.org/x/lint/golint
