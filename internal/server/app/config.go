package app

// Config holds additional configuration that is needed by App. Properties of
// this struct are not only needed for initialization, they are refered to
// during operations.
type Config struct {
	ServerURL string
}
